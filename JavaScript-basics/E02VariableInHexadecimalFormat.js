/**
 * Created by user on 25.10.2016 г..
 */
function problemSolved(lines){

    for (let l of lines) {
        //var numHex = lines.split(' ')[0];
        var numDec =  parseInt(l, 16);
        console.log(numDec);
    }

}
problemSolved("0xFE, 0x37, 0x10");